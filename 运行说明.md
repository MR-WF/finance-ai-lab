# 项目① · Week 1 最小 Demo 运行说明

## 一、LangChain 心智模型（5 个积木）
先把这套记牢，后面就不慌了：

| 积木 | 作用 | 本项目用什么 |
|------|------|------|
| **Document Loader** | 读取文件（txt/pdf/网页） | `TextLoader` / `PyPDFLoader` |
| **Text Splitter** | 把长文切成小块 | `RecursiveCharacterTextSplitter` |
| **Embeddings** | 把文字变成向量（数字） | `HuggingFaceEmbeddings` (bge-small-zh，**本地**) |
| **Vector Store** | 存向量、按相似度检索 | `Chroma`（**本地**） |
| **LLM + Chain** | 用检索到的片段生成答案 | `ChatOpenAI`(DeepSeek) + `RetrievalQA` |

整条链路：**文档 → 切分 → 向量化入库 → 提问时检索 top-k → 拼进 prompt → LLM 生成带引用的答案**。

## 二、安装依赖
```bash
pip install -r requirements.txt
```

## 三、配置 DeepSeek API Key
1. 去 https://platform.deepseek.com 注册并创建 API Key（以 `sk-` 开头）。
2. 设置环境变量（**切勿把 key 写进代码或提交到 GitHub**）：
   - bash / Git Bash：`export DEEPSEEK_API_KEY="sk-xxxx"`
   - Windows PowerShell：`$env:DEEPSEEK_API_KEY="sk-xxxx"`
3. 进阶：用 `python-dotenv` 读 `.env` 文件，并把 `.env` 加进 `.gitignore`。

## 四、准备示例数据
新建 `data/sample.txt`，随便写几段"合规要求"文字即可（先跑通流程）。
例如：
```
公司应建立反洗钱内部控制制度。
所有客户须完成身份识别与风险等级划分。
重大违规行为应在 5 个工作日内上报监管机构。
```
后续 Week 2 换成央行 / 国家金融监督管理总局官网公开的规章 PDF。

## 五、运行
```bash
python rag_minimal.py
```
看到【回答】和【引用来源】即成功。

## 六、常见排错
- `未检测到 DEEPSEEK_API_KEY` → 环境变量没设好，重设后重启终端。
- 网络超时 / 429 → DS 有速率限制，稍等重试；或检查网络/代理。
- 首次卡在下载 bge 模型 → 正常，约 130MB，耐心等；可配国内镜像源加速。
- `base_url` 路径报错 → 尝试改为 `https://api.deepseek.com/v1`。

## 七、下一步（Week 2+）
1. 换 `PyPDFLoader` 读真实监管 PDF。
2. 加 **重排（rerank）** 提升召回精度。
3. 答案里标 `[1][2]` 引用编号并链接回原文段落（引用溯源 UI）。
4. 接 `Streamlit` 做可视化界面，Week 4 推 GitHub 公开。

## 八、.gitignore 建议（别提交这些）
```
chroma_db/
.env
__pycache__/
*.pyc
```
